/**
 * Tool: {{TOOL_NAME}}
 * Upstream operation: {{PROVENANCE}}
 *
 * Generated by mcpgen. The input schema and handler body below were synthesized
 * from the source spec. Review the handler before deploying (see SECURITY.md).
 */
import { z } from "zod";
import type { ToolModule, ToolContext, ToolResult } from "../runtime.js";

export const inputSchema = {{INPUT_SHAPE}};

async function handler(
  args: Record<string, unknown>,
  ctx: ToolContext,
): Promise<ToolResult> {
{{HANDLER_BODY}}
}

export const tool: ToolModule = {
  name: {{TOOL_NAME_LITERAL}},
  title: {{TOOL_TITLE_LITERAL}},
  description: {{TOOL_DESCRIPTION_LITERAL}},
  inputSchema,
  handler,
};
