# Generated by mcpgen — Render Blueprint for {{PACKAGE_NAME}}.
#
# Push this repo to GitHub, then on Render: New + → Blueprint → pick the repo.
# Render builds the Dockerfile and wires the health check below. Secrets marked
# `sync: false` are entered in the dashboard, never committed.
services:
  - type: web
    name: {{PACKAGE_NAME}}
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: MCPGEN_TRANSPORT
        value: http
      - key: MCPGEN_HTTP_PORT
        value: "3000"
      - key: MCPGEN_API_BASE_URL
        sync: false
{{RENDER_SECRET_ENV}}
