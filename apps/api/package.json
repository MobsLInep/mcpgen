{
  "name": "@mcpgen/api",
  "version": "0.0.0",
  "private": true,
  "description": "Thin backend the mcpgen web UI calls (placeholder).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@mcpgen/core": "workspace:*"
  }
}
